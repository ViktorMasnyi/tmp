import { expect, test } from "bun:test";

test('2 + 2', () => {
  expect(2 + 2).toBe(4);
});

// create API test to test /blocks endpoint
test('POST /blocks', async () => {
    // create a mock fastify instance
    // create a mock pool instance
    // create a mock request object
    // create a mock reply object
    // call the handler function with the mock request and reply objects
    // assert that the reply object has been called with the expected value
});
